extends default

block content
  div.container
    each socket in sockets
      div.socket
        h2.socket-name
          | #{socket.name} 
        h2.socket-name  
          | #{socket.uid}
        h2.socket-name  
          | #{socket.status}
        a.button(href=`/?uid=${socket.uid}`)
          | Toggle